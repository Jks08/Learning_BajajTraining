from collections import * 
import pandas as pd
import numpy as np
import time
import os
from sklearn.model_selection import train_test_split
from sklearn.metrics import confusion_matrix
import keras


name = input('Enter Name: ')
print(f'Hello {name}. Nice to meet you. Good Morning!')
